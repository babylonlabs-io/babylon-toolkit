/**
 * Shared types for LoanCard components (Borrow, Repay)
 */

/**
 * Represents a borrowable/repayable asset in the Aave integration
 */
export interface Asset {
  name: string;
  symbol: string;
  icon: string;
}
